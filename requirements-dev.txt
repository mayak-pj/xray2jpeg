-r requirements.txt
# pytest 8.3.x — последняя ветка с поддержкой Python 3.8.
pytest==8.3.5
pefile==2024.8.26
# PyInstaller 6.22.3 поддерживает Python 3.8; bootloader собран под уровень Windows 7.
pyinstaller==6.22.3; sys_platform == "win32"
